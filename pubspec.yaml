name: soft_skills_tefa
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  # 🎨 Animasi & Chart
  lottie: ^3.1.0
  fl_chart: ^1.1.0
  cupertino_icons: ^1.0.8

  # 🔥 Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.7.0        # opsional: login
  cloud_firestore: ^5.4.0      # opsional: database
  firebase_storage: ^12.3.0    # opsional: simpan file (pdf, excel, dll)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/animations/
